<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberVO">
		<insert id="insert" parameterType="insertCommand">
		INSERT INTO member
		(member_seq, member_id, member_password, member_nickname, member_birth_day, member_reg_day)
		VALUES
		(member_seq.nextval, #{memberId}, #{memberPw}, #{memberNickname}, #{memberBirthDay}, SYSDATE)
	</insert>
	
</mapper>